# 依赖与构建产物（由镜像内 npm ci / vite build 重新生成）
node_modules
**/node_modules
**/dist
packages/web/dist
packages/web-collab/dist

# 运行时数据（务必排除，使用挂载卷持久化）
data
backups

# VCS / 编辑器 / 系统
.git
**/.git
**/.DS_Store
**/*.log
**/.workbuddy

# 文档（不进镜像）
**/*.md
