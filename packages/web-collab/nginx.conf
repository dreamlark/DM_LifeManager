server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA 回退：刷新子路由不 404
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 带 hash 的资源名已内容寻址，长缓存
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}
